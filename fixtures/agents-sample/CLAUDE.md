# Project instructions

Run `npm test` before every commit. Use conventional commits.

## Deployment

Deploy only from main. The deploy command is `npm run deploy` and it requires
the STAGING_URL environment variable. Never deploy on Fridays.

## Testing

All new features need unit tests. Integration tests live in tests/integration.
